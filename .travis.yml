sudo: false
dist: trusty
language: java

cache:
  directories:
  - $HOME/.m2

install:
  - pushd vaadin-bom
  - mvn install
  - popd
  - pushd vaadin
  - mvn install
  - popd
  - pushd vaadin-platform-test
  - mvn install
  - popd

script:
 - pushd vaadin-platform-test
 - mvn test -B
 - popd
