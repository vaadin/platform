<?xml version="1.0" encoding="UTF-8"?>
<!-- Logging config for the test (failsafe) JVM. The application itself logs
     only to a file via logback-spring.xml; the tests keep concise console
     output so CI shows test progress and the benchmark's ##teamcity lines. -->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} -- %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
