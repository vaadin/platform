version: 2.1

orbs:
  shellcheck: circleci/shellcheck@1.3.16
  maven: circleci/maven@0.0.12

workflows:
  maven_test:
    jobs:
      - maven/test # checkout, build, test, and upload test results
  shellcheck:
    jobs:
      - shellcheck/check:
        pre-steps:
          - run:
            name: "Install git and SSH client"
            command: | scripts/generateBoms.sh
          

